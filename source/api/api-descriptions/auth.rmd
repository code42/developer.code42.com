Bearer authentication tokens must be used in the headers of most API requests to validate that your account has the proper permissions to take its associated action.

If your organization uses two-factor authentication for local users, you must also include a `totp-auth` header value containing the Time-based One-Time Password (TOTP).

